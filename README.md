# task-manager-ui

## Build Setup

```bash
# Run the container
$ docker compose up -d

# Will be available at https://localhost:3000
```
[Task Manager UI](https://localhost:3000) (http://localhost:300)
